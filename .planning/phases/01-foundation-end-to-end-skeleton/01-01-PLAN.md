---
phase: 01-foundation-end-to-end-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.mjs
  - postcss.config.js
  - tailwind.config.ts
  - vitest.config.ts
  - .eslintrc.json
  - .gitignore
  - .env
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - public/logo-kodryx.png
  - prisma/schema.prisma
  - lib/prisma.ts
  - lib/types/assessment.ts
  - lib/constants/companyProfile.ts
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "Running `npm run dev` serves http://localhost:3000 with KODRYX Navy (#0E2A3A) / Gold (#C9A24D) colors and Poppins/Inter fonts visibly applied"
    - "A SQLite database file exists containing an Assessment table matching the design spec's model"
    - "`npm run test` executes via Vitest and exits 0"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "KODRYX brand color/font/radius tokens available project-wide"
      contains: "kx-navy"
    - path: "prisma/schema.prisma"
      provides: "Assessment model matching the design spec"
      contains: "model Assessment"
    - path: "lib/prisma.ts"
      provides: "Shared PrismaClient singleton (dev hot-reload safe)"
    - path: "lib/types/assessment.ts"
      provides: "Shared TS contracts (WizardAnswers, ScoringInput, CategoryScores, Stage) consumed by later plans"
    - path: "app/page.tsx"
      provides: "Branded landing page with Start Assessment CTA"
  key_links:
    - from: "app/layout.tsx"
      to: "tailwind.config.ts theme tokens"
      via: "className references to kx-navy / font-display utilities"
      pattern: "kx-navy|font-display"
    - from: "app/page.tsx"
      to: "/assessment route"
      via: "next/link Link href"
      pattern: "href=\"/assessment\""
---

<objective>
Scaffold the KAMA Next.js 14 project, wire the KODRYX brand system into Tailwind,
and stand up the Prisma/SQLite data layer with the full `Assessment` model from
the design spec — the foundation every later plan in this phase (and every
later phase) builds on.

Purpose: Nothing else in this phase can be built without a running project,
brand tokens, and a persisted data model. This plan is Wave 1 — everything
else in Phase 1 depends on it.
Output: A working `npm run dev` project with a brand-styled landing page,
a Prisma schema migrated to SQLite, and shared TS contracts for downstream plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md
@../00 Kodryx AI Design System/README.md
@../00 Kodryx AI Design System/colors_and_type.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy check before install</name>
  <files>None — gate only; Task 1 and Task 3 create package.json and perform the installs once this gate is approved</files>
  <read_first>
    docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md (Tech stack section)
  </read_first>
  <action>
    Present the package list below to the human and block on explicit
    approval before Task 1 or Task 3 runs any `npm install` command. Do not
    run any install step until the resume-signal is received. No files are
    created or modified by this task itself.
  </action>
  <what-built>
    No RESEARCH.md exists for this project (research was explicitly skipped
    per user direction), so per the package legitimacy gate every package
    this phase installs is treated as [ASSUMED] pending human confirmation.
    This checkpoint lists every new package Task 1 and Task 3 will install,
    before any install runs.
  </what-built>
  <how-to-verify>
    Confirm each of the following resolves to the real, official package on
    the npm registry (not a typosquat) by checking the linked page:
    - next — https://www.npmjs.com/package/next
    - react — https://www.npmjs.com/package/react
    - react-dom — https://www.npmjs.com/package/react-dom
    - typescript — https://www.npmjs.com/package/typescript
    - tailwindcss — https://www.npmjs.com/package/tailwindcss
    - postcss — https://www.npmjs.com/package/postcss
    - autoprefixer — https://www.npmjs.com/package/autoprefixer
    - eslint — https://www.npmjs.com/package/eslint
    - eslint-config-next — https://www.npmjs.com/package/eslint-config-next
    - @types/node — https://www.npmjs.com/package/@types/node
    - @types/react — https://www.npmjs.com/package/@types/react
    - @types/react-dom — https://www.npmjs.com/package/@types/react-dom
    - prisma — https://www.npmjs.com/package/prisma
    - @prisma/client — https://www.npmjs.com/package/@prisma/client
    - vitest — https://www.npmjs.com/package/vitest
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package that looks wrong</resume-signal>
  <verify>
    <automated>MISSING — human gate, no automated check; approval is the gate itself</automated>
  </verify>
  <acceptance_criteria>
    - Human has explicitly approved the package list above before Task 1 runs any `npm install`
  </acceptance_criteria>
  <done>Human has typed "approved" (or equivalent explicit confirmation) for the full package list</done>
</task>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 App Router project + toolchain</name>
  <files>package.json, tsconfig.json, next.config.mjs, postcss.config.js, tailwind.config.ts, vitest.config.ts, .eslintrc.json, .gitignore, app/layout.tsx, app/page.tsx, app/globals.css</files>
  <read_first>
    docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md (Tech stack, Pages &amp; routes sections)
  </read_first>
  <action>
    The project root already contains `.planning/`, `CLAUDE.md`, `docs/`, and
    the source spec `.txt` file, so it is NOT empty — do not run the
    interactive `create-next-app` CLI (it will refuse or prompt destructively
    against a non-empty directory). Instead, manually author the standard
    Next.js 14 App Router file set: `package.json` with dependencies `next`,
    `react`, `react-dom` and devDependencies `typescript`, `@types/node`,
    `@types/react`, `@types/react-dom`, `tailwindcss`, `postcss`,
    `autoprefixer`, `eslint`, `eslint-config-next`, `vitest` (install with
    `npm install next react react-dom` then
    `npm install -D typescript @types/node @types/react @types/react-dom tailwindcss postcss autoprefixer eslint eslint-config-next vitest`).
    Add npm scripts: `dev` (`next dev`), `build` (`next build`), `start`
    (`next start`), `lint` (`next lint`), `test` (`vitest run`).
    Create `tsconfig.json` with `strict: true`, `jsx: preserve`,
    `moduleResolution: bundler`, path alias `@/*` mapped to `./*`.
    Create `next.config.mjs` exporting a minimal `nextConfig = {}` via
    `export default nextConfig`. Create `postcss.config.js` with `tailwindcss`
    and `autoprefixer` plugins. Create a base `tailwind.config.ts` (content
    globs `./app/**/*.{ts,tsx}` and `./components/**/*.{ts,tsx}`, empty
    `theme.extend` for now — Task 2 fills in brand tokens). Create
    `vitest.config.ts` using `defineConfig` from `vitest/config` with
    `test.environment` set to `'node'` (pure-logic tests only in this phase,
    no DOM needed). Create `.eslintrc.json` extending `next/core-web-vitals`.
    Create `.gitignore` covering `node_modules`, `.next`, `*.db`,
    `*.db-journal`, `.env`. Create a minimal `app/layout.tsx` (default
    export `RootLayout({ children })` returning `<html lang="en"><body>{children}</body></html>`)
    and a placeholder `app/page.tsx` (default export returning a plain
    `<main>KAMA</main>` — Task 2 replaces this with the branded landing
    page). Create `app/globals.css` with the three Tailwind directives
    (`@tailwind base; @tailwind components; @tailwind utilities;`).
  </action>
  <verify>
    <automated>npm run build 2&gt;&amp;1 | grep -v '^#' | grep -qci "compiled successfully\|✓ compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run dev` starts without error and serves a 200 response for `/`
    - `npm run test` runs Vitest and exits 0 (if Vitest errors on zero test files, add a trivial passing `lib/scaffold.test.ts` asserting `1 + 1 === 2` so the runner has something to run; Task 3 of Plan 01-02 supersedes it with real tests)
    - `npm run lint` runs without configuration errors
  </acceptance_criteria>
  <done>`npm run dev`, `npm run build`, `npm run lint`, and `npm run test` all exit 0</done>
</task>

<task type="auto">
  <name>Task 2: Wire KODRYX brand tokens + branded landing page</name>
  <files>tailwind.config.ts, app/layout.tsx, app/page.tsx, app/globals.css, public/logo-kodryx.png</files>
  <read_first>
    ../00 Kodryx AI Design System/colors_and_type.css
    ../00 Kodryx AI Design System/README.md (Visual foundations, Cards, Corner radii, Layout rules sections)
    tailwind.config.ts
    app/layout.tsx
    app/page.tsx
  </read_first>
  <action>
    Copy `../00 Kodryx AI Design System/assets/logo-kodryx.png` into
    `public/logo-kodryx.png` (binary copy, do not regenerate or re-encode it).
    Extend `tailwind.config.ts`'s `theme.extend.colors` with the exact hex
    values from `colors_and_type.css`: `kx-navy` `#0E2A3A`, `kx-gold`
    `#C9A24D`, `kx-grey` `#6B7280`, `kx-grey-50` `#F7F8FA`, `kx-grey-100`
    `#EEF0F3`, `kx-grey-200` `#D8DCE2`. Extend `theme.extend.borderRadius`
    with `sm: '4px'`, `md: '6px'`, `lg: '8px'`. Extend
    `theme.extend.fontFamily` with `display` and `body` keys pointing at CSS
    variables `var(--font-poppins)` and `var(--font-inter)` respectively
    (with `sans-serif` fallback). In `app/layout.tsx`, import `Poppins` and
    `Inter` from `next/font/google`; instantiate
    `Poppins({ subsets: ['latin'], weight: ['600','700'], variable: '--font-poppins' })`
    and `Inter({ subsets: ['latin'], weight: ['400','500','600'], variable: '--font-inter' })`;
    apply both `.variable` class names plus `font-body` and a white
    background (`bg-white text-kx-navy`) to the `<body>` tag; set the page
    `metadata` export `{ title: 'KODRYX AI Maturity Assessment', description: 'AI readiness assessment and executive report' }`.
    In `app/globals.css`, after the Tailwind directives, set `body { margin: 0; }`,
    then port the component utility classes from
    `../00 Kodryx AI Design System/colors_and_type.css` verbatim (copy the
    `.kx-caption`, `.kx-eyebrow`, `.kx-metric`, and `.kx-divider-gold` rule
    blocks exactly as written there, replacing their `var(--kx-*)`
    references with the literal hex values `#0E2A3A` (navy) / `#C9A24D`
    (gold) / `#6B7280` (grey) since this project defines tokens in
    `tailwind.config.ts` rather than CSS custom properties) — these classes
    are referenced as bare classNames in Plan 01-03 and Plan 01-04 and MUST
    resolve to real, visible CSS, not a Tailwind-JIT no-op. Do not add any
    other competing base styles beyond this — Tailwind utility classes drive
    everything else. Replace the placeholder `app/page.tsx` with a branded
    landing page: centered column, max-width `~640px`, the logo
    (`next/image`, `src="/logo-kodryx.png"`, fixed width e.g. 120px,
    `alt="Kodryx AI"`), an `h1` reading "KODRYX AI Maturity Assessment™" in
    `font-display text-kx-navy`, one sentence of body copy in
    `font-body text-kx-grey` describing the assessment (5-8 minutes,
    executive-grade report), and a `next/link` `Link href="/assessment"`
    styled as a solid button: `bg-kx-navy text-white rounded-md px-6 py-3
    hover:bg-kx-navy/90 transition-colors duration-200` reading "Start
    Assessment". No shadows, no gradients; a hairline border only if a
    container box is used (`border border-kx-grey-100 rounded-lg`).
  </action>
  <verify>
    <automated>npm run build 2&gt;&amp;1 | grep -v '^#' | grep -qci "compiled successfully\|✓ compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `tailwind.config.ts` contains the six `kx-*` color keys with the exact hex values listed above
    - `public/logo-kodryx.png` exists and is byte-identical to the design system source file
    - `app/page.tsx` renders an `h1` with the exact string "KODRYX AI Maturity Assessment™" and a link with `href="/assessment"`
    - `app/globals.css` contains `.kx-caption`, `.kx-eyebrow`, `.kx-metric`, and `.kx-divider-gold` rule blocks with real color/font declarations (not empty rules)
    - Visiting `/` in the browser (via `npm run dev`) shows white background, navy heading text, and the gold-accented "Start Assessment" button with no console errors
  </acceptance_criteria>
  <done>`/` renders the branded landing page with correct colors/fonts and a working link to `/assessment`</done>
</task>

<task type="auto">
  <name>Task 3: Prisma/SQLite data model + shared TS contracts</name>
  <files>prisma/schema.prisma, .env, lib/prisma.ts, lib/types/assessment.ts, lib/constants/companyProfile.ts</files>
  <read_first>
    docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md (Data model (Prisma), Scoring engine sections)
  </read_first>
  <action>
    Install `prisma` (devDependency) and `@prisma/client` (dependency) via
    `npm install @prisma/client` and `npm install -D prisma`. Create
    `prisma/schema.prisma` with `generator client { provider = "prisma-client-js" }`,
    `datasource db { provider = "sqlite", url = env("DATABASE_URL") }`, and a
    `model Assessment` matching the design spec's field names and JSON
    columns exactly: `id String @id @default(cuid())`, `createdAt DateTime @default(now())`,
    `contactName String`, `contactEmail String`, `contactPhone String?`,
    `companyName String`, `companyProfile Json`, `departments Json?`,
    `techStack Json?`, `dataReadiness Json?`, `aiAdoption Json?`,
    `automation Json?`, `agentInterest Json?`, `security Json?`,
    `employeeReadiness Json?`, `categoryScores Json`, `totalScore Int`,
    `stage String`, `dataCompleteness String?`, `benchmark Json?`,
    `swot Json?`, `opportunityMatrix Json?`, `risk Json?`, `roadmap Json?`,
    `roi Json?`, `executiveSummary String?`. The nine sections not yet
    collected by Phase 1's 2-step wizard (`departments` through
    `employeeReadiness`) and the report fields not yet generated
    (`dataCompleteness` through `executiveSummary`) are nullable — Phase 1
    inserts them as `Prisma.JsonNull` (for the `Json?` columns) or plain
    `null` (for `executiveSummary`/`dataCompleteness`); `companyProfile`,
    `categoryScores`, `totalScore`, and `stage` are required because Phase 1
    computes and stores all four via the scoring engine (Plan 01-02). Create
    `.env` with `DATABASE_URL="file:./dev.db"` (resolves relative to
    `prisma/schema.prisma`, i.e. `prisma/dev.db`). Run
    `npx prisma migrate dev --name init` to generate the migration and create
    the SQLite file, then `npx prisma generate` to emit the client. Create
    `lib/prisma.ts` exporting a singleton `PrismaClient`: check
    `globalThis` for a cached `prisma` property and reuse it if present,
    otherwise instantiate `new PrismaClient()`; when
    `process.env.NODE_ENV !== 'production'`, assign the instance back onto
    `globalThis` so Next.js dev hot-reload does not exhaust SQLite
    connections. Create `lib/types/assessment.ts` exporting: `ContactInfo`
    ( `contactName: string`, `contactEmail: string`, `contactPhone?: string`,
    `companyName: string` ), `CompanyProfileAnswers` ( `industry: string`,
    `employeeBand: string`, `revenueBand: string`, `businessModel: string`,
    `leadershipGoals: string[]`, `blendedHourlyCost: number` ),
    `WizardAnswers` ( `contact: ContactInfo`, `companyProfile: CompanyProfileAnswers` ),
    `CategoryKey` (union of the 10 literal category names:
    `'businessProcess' | 'technology' | 'data' | 'aiAdoption' | 'automation' | 'aiAgents' | 'security' | 'people' | 'leadership' | 'innovation'`),
    `CategoryScores` ( `Record&lt;CategoryKey, number&gt;` ), `Stage`
    ( `level: number`, `label: string` ), and `ScoringInput` — the shape the
    scoring engine reads, mirroring every JSON column on `Assessment`:
    `companyProfile: CompanyProfileAnswers` (required) plus the nine
    not-yet-collected sections all typed as optional (`departments?`,
    `techStack?`, `dataReadiness?`, `aiAdoption?`, `automation?`,
    `agentInterest?`, `security?`, `employeeReadiness?`) using shapes that
    match the design spec's per-section JSON comments (e.g.
    `dataReadiness?: { quality: number; security: number; accessibility: number; knowledgeMgmt: number }`,
    `employeeReadiness?: { aiSkills: number; training: number; changeReadiness: number; leadershipSupport: number; innovationCulture: number }`).
    Create `lib/constants/companyProfile.ts` exporting the exact option
    lists the wizard and scoring engine both consume:
    `INDUSTRY_OPTIONS = ['Manufacturing', 'SaaS / Technology', 'Healthcare', 'Retail', 'Financial Services', 'Professional Services', 'Education', 'Other']`,
    `EMPLOYEE_BANDS = ['1-10', '11-50', '51-200', '200+']` (matches the
    design spec's company-size normalization bands),
    `REVENUE_BANDS = ['&lt;$1M', '$1M-$5M', '$5M-$20M', '$20M-$100M', '$100M+']`,
    `BUSINESS_MODEL_OPTIONS = ['B2B', 'B2C', 'SaaS', 'Manufacturing', 'Healthcare', 'Education', 'Retail', 'Services']`
    (exact list from the design spec's flow table), `LEADERSHIP_GOALS = ['Increase Revenue', 'Reduce Operating Costs', 'Improve Operational Efficiency', 'Enhance Customer Experience', 'Gain Competitive Advantage', 'Empower Employee Productivity']`,
    and `INDUSTRY_HOURLY_COST_DEFAULTS: Record&lt;string, number&gt;` mapping
    each `INDUSTRY_OPTIONS` value to a starting blended hourly cost (e.g.
    `Manufacturing: 40, 'SaaS / Technology': 75, Healthcare: 65, Retail: 35, 'Financial Services': 70, 'Professional Services': 60, Education: 45, Other: 45`),
    used to prefill Step 1's blended hourly cost field per WIZ-02.
  </action>
  <verify>
    <automated>npx prisma validate &amp;&amp; test -f prisma/dev.db &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` reports the schema is valid
    - `prisma/dev.db` exists on disk after migration
    - `prisma/migrations/*/migration.sql` contains `CREATE TABLE "Assessment"`
    - `lib/types/assessment.ts` exports `WizardAnswers`, `ScoringInput`, `CategoryKey`, `CategoryScores`, and `Stage`
    - `lib/constants/companyProfile.ts` exports all six named constants listed above
  </acceptance_criteria>
  <done>Prisma schema is valid, `prisma/dev.db` exists with an `Assessment` table, and both shared TS contract files export everything listed above</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine <- npm registry | Package installs (Task 1, Task 3) pull code from the public npm registry |
| Local filesystem | SQLite `dev.db` file readable by anyone with filesystem access to this machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (next, react, prisma, vitest, etc.) | mitigate | Task 0 blocking human-verify checkpoint confirms every package against npmjs.com before install |
| T-01-01 | Information Disclosure | `prisma/dev.db` on local filesystem | accept | Single-user, local-only tool per locked constraint (no hosted deployment, no auth in v1); not exposed over a network in Phase 1 |
</threat_model>

<verification>
- `npm run dev` starts and serves the branded `/` landing page
- `npm run build` compiles with no TypeScript errors
- `npx prisma validate` passes and `prisma/dev.db` exists
- `npm run test` runs Vitest and exits 0
</verification>

<success_criteria>
- Next.js 14 App Router + TypeScript + Tailwind project runs locally via `npm run dev`
- KODRYX brand tokens (Navy, Gold, Poppins/Inter) are wired into the Tailwind theme and visibly applied on `/`
- Prisma + SQLite is configured with the full `Assessment` model from the design spec, migrated
- Shared TS contracts and option-list constants exist for Plans 01-02, 01-03, and 01-04 to consume
</success_criteria>

<output>
Create `.planning/phases/01-foundation-end-to-end-skeleton/01-01-SUMMARY.md` when done
</output>
