---
phase: 02-complete-wizard-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types/assessment.ts
  - lib/constants/wizardOptions.ts
  - components/wizard/wizardReducer.ts
  - components/wizard/wizardReducer.test.ts
  - components/wizard/WizardShell.tsx
  - components/wizard/StepBusinessProcess.tsx
  - components/wizard/StepTechnology.tsx
  - components/wizard/StepDataReadiness.tsx
autonomous: true
requirements: [WIZ-03, WIZ-04, WIZ-05]

must_haves:
  truths:
    - "User can rate all 6 departments (Sales, Marketing, HR, Finance, Operations, Support) with a 0-5 maturity slider and check off department-specific capabilities"
    - "User can select which systems are in use and pick one API maturity level"
    - "User can rate the 4 data-readiness dimensions (Quality, Security, Accessibility, Knowledge Management)"
    - "Navigating Back then Forward across steps 0-4 preserves every section's answers with zero data loss"
  artifacts:
    - path: "lib/types/assessment.ts"
      provides: "WizardAnswers extended with all 10 sections (departments, techStack, dataReadiness, aiAdoption, automation, agentInterest, security, employeeReadiness added as required fields)"
    - path: "lib/constants/wizardOptions.ts"
      provides: "SYSTEMS_OPTIONS, API_MATURITY_OPTIONS, AI_TOOLS_OPTIONS, AI_ADOPTION_FREQUENCY_OPTIONS, AUTOMATION_PROCESS_OPTIONS, AGENT_TYPE_OPTIONS, SECURITY_CHECKLIST_OPTIONS, DEPARTMENT_CONFIG"
    - path: "components/wizard/wizardReducer.ts"
      provides: "TOTAL_WIZARD_STEPS constant, UPDATE_DEPARTMENT/UPDATE_TECH_STACK/UPDATE_DATA_READINESS/UPDATE_AI_ADOPTION/UPDATE_AUTOMATION/UPDATE_AGENT_INTEREST/UPDATE_SECURITY/UPDATE_EMPLOYEE_READINESS actions"
    - path: "components/wizard/StepBusinessProcess.tsx"
      provides: "6-department slider + capability checklist step UI"
    - path: "components/wizard/StepTechnology.tsx"
      provides: "systems multi-select + API maturity select step UI"
    - path: "components/wizard/StepDataReadiness.tsx"
      provides: "4-dimension data readiness slider step UI"
  key_links:
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepBusinessProcess.tsx"
      via: "switch(state.stepIndex) case 2, dispatch({ type: \"UPDATE_DEPARTMENT\" })"
      pattern: "case 2:"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepTechnology.tsx"
      via: "switch(state.stepIndex) case 3, dispatch({ type: \"UPDATE_TECH_STACK\" })"
      pattern: "case 3:"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepDataReadiness.tsx"
      via: "switch(state.stepIndex) case 4, dispatch({ type: \"UPDATE_DATA_READINESS\" })"
      pattern: "case 4:"
---

<objective>
Extend the Phase 1 two-step wizard's shared contracts (types, option-list constants, reducer state machine) to cover all 10 assessment steps in one pass — matching Phase 1's own precedent of building the full `ScoringInput` shape upfront — then wire the first 3 new real steps (Business Process, Technology, Data Readiness) into `WizardShell`, taking the wizard from 2 navigable steps to 5.

Purpose: Every later plan in this phase (02-02, 02-03) only needs to add step components and bump one constant — no further reducer/type rewrites — because the full contract surface (WizardAnswers, all 8 new WizardAction variants, all option-list constants) is defined once, here.
Output: `WizardAnswers` covering all 10 sections; `wizardReducer.ts` with a complete action set and `TOTAL_WIZARD_STEPS` state-machine bound; `lib/constants/wizardOptions.ts`; 3 new step components wired into `WizardShell.tsx` as steps 2-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md
@lib/types/assessment.ts
@lib/scoring/categoryFormulas.ts
@lib/constants/companyProfile.ts
@components/wizard/WizardShell.tsx
@components/wizard/wizardReducer.ts
@components/wizard/wizardReducer.test.ts
@components/wizard/StepCompanyProfile.tsx
</context>

<interfaces>
<!-- Existing shapes from lib/types/assessment.ts -- reuse directly, do not redefine. -->

DepartmentAnswer: `{ rating: number; capabilities: string[] }`
Departments: `Record<"sales"|"marketing"|"hr"|"finance"|"operations"|"support", DepartmentAnswer>`
TechStackAnswers: `{ systems: string[]; apiMaturity: string }`
DataReadinessAnswers: `{ quality: number; security: number; accessibility: number; knowledgeMgmt: number }`

<!-- Denominators + option order already load-bearing in lib/scoring/categoryFormulas.ts -- new UI option lists MUST match these exactly (string values AND order), since technologyRatio/aiAdoptionRatio use indexOf(value)/3 and the other ratios use length / TOTAL_*_OPTIONS: -->

- TOTAL_SYSTEMS_OPTIONS = 8
- API_MATURITY_LEVELS = ["Legacy", "Basic", "Modern", "Enterprise"]
- TOTAL_AI_TOOLS_OPTIONS = 10
- AI_ADOPTION_FREQUENCY_LEVELS = ["Never", "Weekly", "Daily", "Company-wide"]
- TOTAL_AUTOMATION_PROCESSES_OPTIONS = 9
- TOTAL_SECURITY_CHECKLIST_OPTIONS = 11

<!-- Existing WizardShell.tsx step-rendering pattern (2 steps) -- Task 2/3 convert this to a switch over 5 cases: -->

Current: `const TOTAL_STEPS = 2;` then `{state.stepIndex === 0 ? <StepRegistration .../> : <StepCompanyProfile .../>}`
New: import `TOTAL_WIZARD_STEPS` from `./wizardReducer`; replace the ternary with `switch (state.stepIndex) { case 0: ...; case 1: ...; case 2: ...; case 3: ...; case 4: ...; }`; `isLastStep = state.stepIndex === TOTAL_WIZARD_STEPS - 1`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend WizardAnswers, wizardOptions constants, and wizardReducer state machine for all 10 steps</name>
  <files>lib/types/assessment.ts, lib/constants/wizardOptions.ts, components/wizard/wizardReducer.ts, components/wizard/wizardReducer.test.ts</files>
  <read_first>lib/types/assessment.ts, lib/constants/companyProfile.ts, lib/scoring/categoryFormulas.ts, components/wizard/wizardReducer.ts, components/wizard/wizardReducer.test.ts, docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md (Assessment flow content table, rows 2-9), KODRYX AI Maturity Assessment™.txt at the project root (Step 2 per-department question lists)</read_first>
  <behavior>
    - initialWizardState.stepIndex stays 0; TOTAL_WIZARD_STEPS is exported as 5 (reflects the 5 steps wired by the end of this plan: Registration, CompanyProfile, BusinessProcess, Technology, DataReadiness).
    - NEXT from stepIndex 4 does not advance past 4 (Math.min(state.stepIndex + 1, TOTAL_WIZARD_STEPS - 1)); BACK from 0 does not go below 0.
    - Dispatching UPDATE_DEPARTMENT with department "sales" and payload { rating: 3 } updates only answers.departments.sales.rating, leaving marketing/hr/finance/operations/support at their initial defaults ({ rating: 0, capabilities: [] }).
    - Dispatching UPDATE_TECH_STACK with payload { apiMaturity: "Modern" } updates only answers.techStack.apiMaturity, leaving answers.techStack.systems and answers.dataReadiness untouched.
  </behavior>
  <action>
    In lib/types/assessment.ts, extend the WizardAnswers interface to add 8 new required members using the interfaces already defined in the same file: departments (Departments), techStack (TechStackAnswers), dataReadiness (DataReadinessAnswers), aiAdoption (AiAdoptionAnswers), automation (AutomationAnswers), agentInterest (AgentInterestAnswers), security (SecurityAnswers), employeeReadiness (EmployeeReadinessAnswers). Do not modify ScoringInput -- its 8 matching fields are already optional there, and a fully-populated WizardAnswers value satisfies that shape structurally (extra required fields on an object still satisfy an interface where those same fields are declared optional).

    Create lib/constants/wizardOptions.ts exporting, as readonly arrays matching the exact strings and ORDER load-bearing in categoryFormulas.ts (see interfaces section above):
    - SYSTEMS_OPTIONS (8 items): CRM, ERP, HRMS, Accounting, Cloud, Document Storage, Communication, Automation Tools
    - API_MATURITY_OPTIONS (4 items, must match API_MATURITY_LEVELS order exactly): Legacy, Basic, Modern, Enterprise
    - AI_TOOLS_OPTIONS (10 items): ChatGPT, Claude, Gemini, Copilot, Perplexity, Cursor, GitHub Copilot, AI Meeting Assistant, AI Chatbot, AI Coding
    - AI_ADOPTION_FREQUENCY_OPTIONS (4 items, must match AI_ADOPTION_FREQUENCY_LEVELS order exactly): Never, Weekly, Daily, Company-wide
    - AUTOMATION_PROCESS_OPTIONS (9 items): Email, Lead routing, Invoices, Purchase approvals, Onboarding, Reporting, Notifications, Scheduling, Customer comms
    - AGENT_TYPE_OPTIONS (11 items): Sales, Marketing, HR, Finance, Legal, Knowledge, Support, Operations, Voice AI, Document AI, Vision AI
    - SECURITY_CHECKLIST_OPTIONS (11 items): MFA, SSO, Backups, Data Encryption, GDPR, HIPAA, ISO, Audit Logs, AI Governance, Prompt Security, Data Privacy
    - DEPARTMENT_CONFIG: an array of 6 objects typed { key: keyof Departments; label: string; capabilities: readonly string[] } (import Departments from ../types/assessment), in this exact order/content:
      1. key "sales", label "Sales", capabilities [CRM, Proposal Creation, Sales Forecasting, Follow-up Automation, Sales Analytics]
      2. key "marketing", label "Marketing", capabilities [Social Media, Content Generation, SEO, Email Marketing, Campaign Analytics]
      3. key "hr", label "HR", capabilities [Recruitment, Resume Screening, Payroll, Employee Onboarding, Performance Reviews]
      4. key "finance", label "Finance", capabilities [Invoice Processing, Expense Approvals, Financial Reports, Collections, Budget Planning]
      5. key "operations", label "Operations", capabilities [SOP Documentation, Workflow Automation, Vendor Management, Inventory Management, Compliance]
      6. key "support", label "Customer Support", capabilities [Helpdesk, Email Support, Chatbot, Knowledge Base, SLA Monitoring]

    In components/wizard/wizardReducer.ts: change WizardState.stepIndex type from `0 | 1` to `number`. Export `export const TOTAL_WIZARD_STEPS = 5;`. Extend initialWizardState.answers with defaults for all 8 new sections: departments built from DEPARTMENT_CONFIG (each key maps to { rating: 0, capabilities: [] }), techStack { systems: [], apiMaturity: "" }, dataReadiness { quality: 0, security: 0, accessibility: 0, knowledgeMgmt: 0 }, aiAdoption { toolsInUse: [], frequency: "" }, automation { processesAutomated: [] }, agentInterest { agentTypes: [] }, security { checklist: [] }, employeeReadiness { aiSkills: 0, training: 0, changeReadiness: 0, leadershipSupport: 0, innovationCulture: 0 }. Add 8 new WizardAction union members and reducer case branches -- UPDATE_DEPARTMENT (payload shape { department: keyof Departments; payload: Partial<DepartmentAnswer> }, merges only into answers.departments[department]), UPDATE_TECH_STACK, UPDATE_DATA_READINESS, UPDATE_AI_ADOPTION, UPDATE_AUTOMATION, UPDATE_AGENT_INTEREST, UPDATE_SECURITY, UPDATE_EMPLOYEE_READINESS -- each following the exact immutable-merge pattern already used by UPDATE_CONTACT/UPDATE_COMPANY_PROFILE (spread the existing sub-object, then spread the payload on top). Change the NEXT case to Math.min(state.stepIndex + 1, TOTAL_WIZARD_STEPS - 1) and BACK to Math.max(state.stepIndex - 1, 0).

    In components/wizard/wizardReducer.test.ts: the two existing boundary tests ("NEXT advances stepIndex from 0 to 1 and does not advance past 1" and "BACK returns stepIndex from 1 to 0") hardcode the old 2-step boundary -- rewrite them to dispatch NEXT 4 times from initialWizardState to reach stepIndex 4, assert a 5th NEXT stays at 4, then dispatch BACK 4 times to reach 0 and assert a 5th BACK stays at 0. Add 2 new test cases per the behavior block above (UPDATE_DEPARTMENT isolation, UPDATE_TECH_STACK isolation).
  </action>
  <verify>
    <automated>npx vitest run components/wizard/wizardReducer.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run components/wizard/wizardReducer.test.ts` exits 0 with all tests (including the 2 rewritten boundary tests and 2 new tests) passing
    - `npx tsc --noEmit` exits 0 (no type errors across the project)
    - grep for departments/techStack/dataReadiness/aiAdoption/automation/agentInterest/security/employeeReadiness inside the WizardAnswers interface body in lib/types/assessment.ts reports at least 8 matches
  </acceptance_criteria>
  <done>WizardAnswers, wizardOptions.ts, and wizardReducer.ts compile cleanly; all 8 new UPDATE_* actions merge correctly into their own sub-object without touching siblings; TOTAL_WIZARD_STEPS=5 governs NEXT/BACK clamping.</done>
</task>

<task type="auto">
  <name>Task 2: Build StepBusinessProcess and wire it in as step 2 (WIZ-03)</name>
  <files>components/wizard/StepBusinessProcess.tsx, components/wizard/WizardShell.tsx</files>
  <read_first>components/wizard/StepCompanyProfile.tsx, lib/constants/wizardOptions.ts, lib/types/assessment.ts, components/wizard/WizardShell.tsx, components/wizard/wizardReducer.ts</read_first>
  <action>
    Create components/wizard/StepBusinessProcess.tsx as a "use client" component following StepCompanyProfile.tsx's exact prop/effect pattern, with props { values: Departments; onChange: (department: keyof Departments, payload: Partial<DepartmentAnswer>) => void; onValidityChange: (valid: boolean) => void }. Render DEPARTMENT_CONFIG.map(...) from lib/constants/wizardOptions.ts, one block per department containing: an h3 label (font-display text-kx-navy text-lg font-semibold) with dept.label; a range slider (input type="range" min={0} max={5} step={1}, styled w-full accent-kx-gold) bound to values[dept.key].rating, calling onChange(dept.key, { rating: Number(e.target.value) }), with a kx-caption readout showing "{value} / 5"; and a checkbox list of dept.capabilities (checkbox className="accent-kx-navy", matching the leadershipGoals toggle pattern in StepCompanyProfile.tsx exactly) calling onChange(dept.key, { capabilities: next }) where next toggles membership in values[dept.key].capabilities. Validity effect always calls onValidityChange(true) (a slider at 0 and zero checked capabilities are legitimate answers per the design spec's 0-5 scale -- no blocking required-field check for this step).

    Edit components/wizard/WizardShell.tsx: remove the local `const TOTAL_STEPS = 2;` and import TOTAL_WIZARD_STEPS from ./wizardReducer instead; replace the two-way ternary rendering StepRegistration/StepCompanyProfile with a switch (state.stepIndex) covering case 0 (StepRegistration, unchanged), case 1 (StepCompanyProfile, unchanged), case 2 (StepBusinessProcess wired to values={state.answers.departments} and onChange={(department, payload) => dispatch({ type: "UPDATE_DEPARTMENT", department, payload })}), and a default: return null; fallback (temporary -- steps 3-4 are wired in Task 3 of this same plan; the fallback is never reachable once Task 3 completes since TOTAL_WIZARD_STEPS=5 and cases 0-4 will all exist). Update isLastStep to state.stepIndex === TOTAL_WIZARD_STEPS - 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - grep of `case 2:` in components/wizard/WizardShell.tsx returns a match rendering StepBusinessProcess
    - grep of `DEPARTMENT_CONFIG.map` in components/wizard/StepBusinessProcess.tsx reports at least 1 match, confirming the department config drives rendering (not hardcoded department blocks)
  </acceptance_criteria>
  <done>Business Process step renders all 6 departments, each with a working 0-5 slider and capability checklist, wired into the wizard at step index 2 with no blocking validity requirement.</done>
</task>

<task type="auto">
  <name>Task 3: Build StepTechnology + StepDataReadiness and wire them in as steps 3-4 (WIZ-04, WIZ-05)</name>
  <files>components/wizard/StepTechnology.tsx, components/wizard/StepDataReadiness.tsx, components/wizard/WizardShell.tsx</files>
  <read_first>components/wizard/StepCompanyProfile.tsx, lib/constants/wizardOptions.ts, lib/types/assessment.ts, components/wizard/WizardShell.tsx (post-Task-2 state), components/wizard/StepBusinessProcess.tsx (slider pattern just established)</read_first>
  <action>
    Create components/wizard/StepTechnology.tsx: props { values: TechStackAnswers; onChange: (payload: Partial<TechStackAnswers>) => void; onValidityChange: (valid: boolean) => void }. Render SYSTEMS_OPTIONS (from wizardOptions.ts) as a checkbox multi-select (same toggle pattern as leadershipGoals: onChange({ systems: next })), and API_MATURITY_OPTIONS as a required select (disabled placeholder option "Select a level", selectClassName copied from StepCompanyProfile.tsx's pattern) bound to values.apiMaturity. Validity effect: onValidityChange(values.apiMaturity.trim().length > 0) (systems selection is optional -- 0 systems selected is a legitimate answer for a very early-stage company).

    Create components/wizard/StepDataReadiness.tsx: props { values: DataReadinessAnswers; onChange: (payload: Partial<DataReadinessAnswers>) => void; onValidityChange: (valid: boolean) => void }. Render 4 range sliders (min={0} max={5} step={1}, w-full accent-kx-gold, same pattern as StepBusinessProcess.tsx's department slider) for quality, security, accessibility, knowledgeMgmt, each with a label and a kx-caption "{value} / 5" readout. Validity effect always calls onValidityChange(true) (all 4 sliders always have a default value -- no blocking requirement, matching StepBusinessProcess.tsx's rule).

    Edit components/wizard/WizardShell.tsx: add case 3 rendering StepTechnology wired to values={state.answers.techStack} / onChange={(payload) => dispatch({ type: "UPDATE_TECH_STACK", payload })}; add case 4 rendering StepDataReadiness wired to values={state.answers.dataReadiness} / onChange={(payload) => dispatch({ type: "UPDATE_DATA_READINESS", payload })}; remove the default: return null; fallback added in Task 2 (no longer reachable -- cases 0-4 now cover the full 0..TOTAL_WIZARD_STEPS-1 range).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, and `npm run test` all exit 0
    - grep of `case 0:`, `case 1:`, `case 2:`, `case 3:`, `case 4:` in components/wizard/WizardShell.tsx confirms all 5 cases present and no `default: return null;` fallback remains
    - TOTAL_WIZARD_STEPS in components/wizard/wizardReducer.ts equals 5, matching the 5 switch cases exactly
  </acceptance_criteria>
  <done>All 5 first steps (Registration, CompanyProfile, BusinessProcess, Technology, DataReadiness) are real, wired, and navigable via Back/Next with no data loss; ProgressBar shows "Step 5 of 5" at the final step of this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser user input -> React client state (wizardReducer) | Untrusted user input (slider values, checkbox toggles) enters component state; not yet persisted or sent to a server in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-02-01 | Tampering | components/wizard/wizardReducer.ts (client-only state) | accept | No server trust boundary is crossed in this plan -- POST /api/assessments (the actual boundary) is not modified until Plan 02-03, which adds server-side subset/range validation before any of this data is persisted |
| T-02-02 | Information Disclosure | lib/constants/wizardOptions.ts | accept | Static, non-secret option-list constants (department names, tool names) -- no sensitive data |
| T-02-SC | Tampering | npm package installs | n/a | No new npm packages are installed in this plan -- Package Legitimacy Gate does not apply |
</threat_model>

<verification>
Run `npm run build`, `npm run lint`, and `npm run test` from the project root -- all must exit 0. Manually confirm (or via automated SSR check) that `/assessment` now shows "Step 1 of 5" through "Step 5 of 5" as a user clicks Next across Registration -> CompanyProfile -> BusinessProcess -> Technology -> DataReadiness, with Back preserving every prior step's answers.
</verification>

<success_criteria>
- WizardAnswers type covers all 10 sections; ScoringInput requires no changes.
- wizardReducer.ts exposes TOTAL_WIZARD_STEPS=5 and all 8 new UPDATE_* actions, each isolated to its own sub-object.
- StepBusinessProcess, StepTechnology, and StepDataReadiness are real, wired, navigable wizard steps (not stubs).
- `npm run build`, `npm run lint`, and `npm run test` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/02-complete-wizard-input/02-01-SUMMARY.md` when done
</output>
