---
phase: 02-complete-wizard-input
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/wizard/wizardReducer.ts
  - components/wizard/WizardShell.tsx
  - components/wizard/StepAiAdoption.tsx
  - components/wizard/StepAutomation.tsx
  - components/wizard/StepAgentInterest.tsx
autonomous: true
requirements: [WIZ-06, WIZ-07, WIZ-08]

must_haves:
  truths:
    - "User can select which AI tools are in use and pick one usage frequency"
    - "User can select which business processes are automated"
    - "User can select which AI agent types would be valuable to their business"
    - "Navigating Back then Forward across steps 0-7 preserves every section's answers with zero data loss"
  artifacts:
    - path: "components/wizard/StepAiAdoption.tsx"
      provides: "AI tools multi-select + frequency select step UI"
    - path: "components/wizard/StepAutomation.tsx"
      provides: "automated-processes multi-select step UI"
    - path: "components/wizard/StepAgentInterest.tsx"
      provides: "AI agent type multi-select step UI (feeds Opportunity Matrix in Phase 4, not a maturity input)"
  key_links:
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepAiAdoption.tsx"
      via: "switch(state.stepIndex) case 5, dispatch({ type: \"UPDATE_AI_ADOPTION\" })"
      pattern: "case 5:"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepAutomation.tsx"
      via: "switch(state.stepIndex) case 6, dispatch({ type: \"UPDATE_AUTOMATION\" })"
      pattern: "case 6:"
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/StepAgentInterest.tsx"
      via: "switch(state.stepIndex) case 7, dispatch({ type: \"UPDATE_AGENT_INTEREST\" })"
      pattern: "case 7:"
---

<objective>
Add the AI Adoption, Automation, and AI Agent Interest steps to the wizard, taking it from 5 navigable steps (end of Plan 02-01) to 8. All reducer actions (`UPDATE_AI_ADOPTION`, `UPDATE_AUTOMATION`, `UPDATE_AGENT_INTEREST`) and option-list constants already exist from Plan 02-01 — this plan only creates the 3 step components and bumps `TOTAL_WIZARD_STEPS`.

Purpose: Complete the "opportunity signal" cluster of the assessment — AI tool usage, automation coverage, and agent-type interest are the inputs that directly drive Phase 4's Opportunity Matrix generation, in addition to feeding the `aiAgents`/`automation`/`aiAdoption` category scores today.
Output: `StepAiAdoption.tsx`, `StepAutomation.tsx`, `StepAgentInterest.tsx` wired into `WizardShell.tsx` as steps 5-7; `TOTAL_WIZARD_STEPS` bumped from 5 to 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-07-16-kodryx-ai-maturity-assessment-design.md
@lib/types/assessment.ts
@lib/constants/wizardOptions.ts
@components/wizard/wizardReducer.ts
@components/wizard/WizardShell.tsx
@components/wizard/StepCompanyProfile.tsx
@components/wizard/StepTechnology.tsx
</context>

<interfaces>
<!-- All established by Plan 02-01 -- this plan only consumes them, no redefinition. -->

From lib/types/assessment.ts:
AiAdoptionAnswers: `{ toolsInUse: string[]; frequency: string }`
AutomationAnswers: `{ processesAutomated: string[] }`
AgentInterestAnswers: `{ agentTypes: string[] }`

From lib/constants/wizardOptions.ts (created in Plan 02-01):
AI_TOOLS_OPTIONS (10 items), AI_ADOPTION_FREQUENCY_OPTIONS (4 items: Never, Weekly, Daily, Company-wide), AUTOMATION_PROCESS_OPTIONS (9 items), AGENT_TYPE_OPTIONS (11 items)

From components/wizard/wizardReducer.ts (created/extended in Plan 02-01):
`export const TOTAL_WIZARD_STEPS = 5;` (this plan bumps it to 8)
Actions already defined: `{ type: "UPDATE_AI_ADOPTION"; payload: Partial<AiAdoptionAnswers> }`, `{ type: "UPDATE_AUTOMATION"; payload: Partial<AutomationAnswers> }`, `{ type: "UPDATE_AGENT_INTEREST"; payload: Partial<AgentInterestAnswers> }` -- each already implemented in the reducer's switch, merging into `answers.aiAdoption` / `answers.automation` / `answers.agentInterest` respectively.

From components/wizard/WizardShell.tsx (post Plan 02-01):
`switch (state.stepIndex) { case 0: ...; case 1: ...; case 2: ...; case 3: ...; case 4: ...; }` with no `default` case (cases 0-4 exactly cover `TOTAL_WIZARD_STEPS = 5`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build StepAiAdoption and wire it in as step 5 (WIZ-06)</name>
  <files>components/wizard/StepAiAdoption.tsx, components/wizard/wizardReducer.ts, components/wizard/WizardShell.tsx</files>
  <read_first>components/wizard/StepTechnology.tsx, lib/constants/wizardOptions.ts, lib/types/assessment.ts, components/wizard/WizardShell.tsx, components/wizard/wizardReducer.ts</read_first>
  <action>
    Create components/wizard/StepAiAdoption.tsx following StepTechnology.tsx's exact structure (it is the closest existing analog: one optional multi-select paired with one required single-select). Props: { values: AiAdoptionAnswers; onChange: (payload: Partial<AiAdoptionAnswers>) => void; onValidityChange: (valid: boolean) => void }. Render AI_TOOLS_OPTIONS (from wizardOptions.ts) as a checkbox multi-select bound to values.toolsInUse (toggle pattern identical to StepTechnology.tsx's systems checklist, calling onChange({ toolsInUse: next })), and AI_ADOPTION_FREQUENCY_OPTIONS as a required select (disabled placeholder option "Select a frequency", selectClassName pattern copied from StepCompanyProfile.tsx/StepTechnology.tsx) bound to values.frequency. Validity effect: onValidityChange(values.frequency.trim().length > 0) -- "Never" is itself a valid, selectable frequency option, so the requirement is only that some option was chosen, not that adoption is non-zero.

    Edit components/wizard/wizardReducer.ts: change `export const TOTAL_WIZARD_STEPS = 5;` to `export const TOTAL_WIZARD_STEPS = 6;` (only this numeric literal changes -- no new action types are needed, UPDATE_AI_ADOPTION already exists from Plan 02-01).

    Edit components/wizard/WizardShell.tsx: add `case 5:` rendering StepAiAdoption wired to `values={state.answers.aiAdoption}` / `onChange={(payload) => dispatch({ type: "UPDATE_AI_ADOPTION", payload })}`; add a temporary `default: return null;` fallback after case 5 (steps 6-7 are wired in Task 2 of this same plan; the fallback is unreachable once Task 2 completes since TOTAL_WIZARD_STEPS will be 8 and cases 0-7 will all exist).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - grep of `case 5:` in components/wizard/WizardShell.tsx returns a match rendering StepAiAdoption
    - grep of `TOTAL_WIZARD_STEPS = 6` in components/wizard/wizardReducer.ts confirms the bump
  </acceptance_criteria>
  <done>AI Adoption step renders the 10-tool checklist and required frequency select, wired into the wizard at step index 5.</done>
</task>

<task type="auto">
  <name>Task 2: Build StepAutomation + StepAgentInterest and wire them in as steps 6-7 (WIZ-07, WIZ-08)</name>
  <files>components/wizard/StepAutomation.tsx, components/wizard/StepAgentInterest.tsx, components/wizard/wizardReducer.ts, components/wizard/WizardShell.tsx</files>
  <read_first>components/wizard/StepAiAdoption.tsx (checkbox multi-select pattern just established), lib/constants/wizardOptions.ts, lib/types/assessment.ts, components/wizard/WizardShell.tsx (post-Task-1 state), components/wizard/wizardReducer.ts</read_first>
  <action>
    Create components/wizard/StepAutomation.tsx: props { values: AutomationAnswers; onChange: (payload: Partial<AutomationAnswers>) => void; onValidityChange: (valid: boolean) => void }. Render AUTOMATION_PROCESS_OPTIONS (9 items) as a checkbox multi-select bound to values.processesAutomated (same toggle pattern as StepAiAdoption.tsx's tools checklist, calling onChange({ processesAutomated: next })). No required field -- validity effect always calls onValidityChange(true) (zero automated processes is a legitimate answer for an early-stage company).

    Create components/wizard/StepAgentInterest.tsx: props { values: AgentInterestAnswers; onChange: (payload: Partial<AgentInterestAnswers>) => void; onValidityChange: (valid: boolean) => void }. Render AGENT_TYPE_OPTIONS (11 items) as a checkbox multi-select bound to values.agentTypes (onChange({ agentTypes: next })). Include an intro paragraph (kx-caption, below the h2 heading) noting this step identifies which AI agent types would be valuable for the business -- matching the design spec's framing that this step captures opportunity/interest rather than current usage. No required field -- validity effect always calls onValidityChange(true).

    Edit components/wizard/wizardReducer.ts: change `export const TOTAL_WIZARD_STEPS = 6;` to `export const TOTAL_WIZARD_STEPS = 8;`.

    Edit components/wizard/WizardShell.tsx: add `case 6:` rendering StepAutomation wired to `values={state.answers.automation}` / `onChange={(payload) => dispatch({ type: "UPDATE_AUTOMATION", payload })}`; add `case 7:` rendering StepAgentInterest wired to `values={state.answers.agentInterest}` / `onChange={(payload) => dispatch({ type: "UPDATE_AGENT_INTEREST", payload })}`; remove the `default: return null;` fallback added in Task 1 (no longer reachable -- cases 0-7 now cover the full 0..TOTAL_WIZARD_STEPS-1 range).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `npm run lint`, and `npm run test` all exit 0
    - grep of `case 6:` and `case 7:` in components/wizard/WizardShell.tsx confirms both present, and no `default: return null;` fallback remains
    - TOTAL_WIZARD_STEPS in components/wizard/wizardReducer.ts equals 8, matching the 8 switch cases (0-7) exactly
  </acceptance_criteria>
  <done>All 8 of the first 8 steps (Registration through AI Agent Interest) are real, wired, and navigable via Back/Next with no data loss; ProgressBar shows "Step 8 of 8" at the final step of this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser user input -> React client state (wizardReducer) | Untrusted user input (checkbox/select selections) enters component state; not yet persisted or sent to a server in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-02-03 | Tampering | components/wizard/wizardReducer.ts (client-only state) | accept | No server trust boundary is crossed in this plan -- POST /api/assessments is not modified until Plan 02-03, which adds server-side subset validation for aiAdoption/automation/agentInterest before persistence |
| T-02-SC | Tampering | npm package installs | n/a | No new npm packages are installed in this plan -- Package Legitimacy Gate does not apply |
</threat_model>

<verification>
Run `npm run build`, `npm run lint`, and `npm run test` from the project root -- all must exit 0. Manually confirm (or via automated SSR check) that `/assessment` now shows "Step 1 of 8" through "Step 8 of 8" as a user clicks Next across all 8 implemented steps, with Back preserving every prior step's answers.
</verification>

<success_criteria>
- StepAiAdoption, StepAutomation, and StepAgentInterest are real, wired, navigable wizard steps (not stubs).
- TOTAL_WIZARD_STEPS = 8, matching exactly 8 switch cases in WizardShell.tsx.
- `npm run build`, `npm run lint`, and `npm run test` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/02-complete-wizard-input/02-02-SUMMARY.md` when done
</output>
