---
phase: 03-industry-size-adjusted-scoring-engine
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/api/assessments/route.ts
  - app/api/assessments/route.test.ts
  - app/results/[id]/page.tsx
autonomous: false
requirements: [ENG-04, ENG-05]

must_haves:
  truths:
    - "A submitted assessment persists a real 'thin' or 'complete' dataCompleteness value instead of the previous hardcoded null"
    - "The results page shows the computed stage's whatThisMeans and whatsNext text directly beneath the stage label"
    - "The fast-pass caption on the results page appears only when the submission's dataCompleteness is 'thin', and is absent when 'complete'"
    - "Two submissions with identical raw ratings but different industries render visibly different total scores on their respective /results/[id] pages"
  artifacts:
    - path: "app/api/assessments/route.ts"
      provides: "POST handler calling computeDataCompleteness(scoringInput) and persisting the real value"
      contains: "computeDataCompleteness("
    - path: "app/results/[id]/page.tsx"
      provides: "Results page rendering stage.whatThisMeans/whatsNext and a conditional completeness caption"
      contains: "stage.whatThisMeans"
  key_links:
    - from: "app/api/assessments/route.ts"
      to: "lib/scoring/completeness.ts"
      via: "computeDataCompleteness(scoringInput)"
      pattern: "computeDataCompleteness\\("
    - from: "app/results/[id]/page.tsx"
      to: "lib/scoring/stage.ts"
      via: "getStage(assessment.totalScore)"
      pattern: "getStage\\("
    - from: "app/results/[id]/page.tsx"
      to: "prisma.assessment.dataCompleteness"
      via: "conditional caption render"
      pattern: "dataCompleteness === \"thin\""
---

<objective>
Wire Plan 03-01's industry/size-adjusted scoring, Plan 03-02's stage
guidance text, and Plan 03-02's data-completeness function into the two
places a real user actually observes them: `POST /api/assessments`
(persisting a real `dataCompleteness` value instead of the current
hardcoded `null`) and `/results/[id]` (showing the stage's guidance text
and a conditional fast-pass caption).

Purpose: Plans 03-01/03-02 built and tested the pure logic in isolation.
`calculateCategoryScores`'s call site in `route.ts` needs zero changes
(its signature didn't grow — only its internal behavior did), but
`dataCompleteness` and the results page display DO need explicit wiring
since they were previously hardcoded/unconditional. This plan is what
makes ROADMAP Phase 3's user-visible success criteria (3 and 4) actually
true.
Output: `route.ts` persisting real `dataCompleteness`; `/results/[id]`
showing stage guidance text and a conditional completeness caption;
human-verified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-industry-size-adjusted-scoring-engine/03-01-SUMMARY.md
@.planning/phases/03-industry-size-adjusted-scoring-engine/03-02-SUMMARY.md
@app/api/assessments/route.ts
@app/api/assessments/route.test.ts
@app/results/[id]/page.tsx
</context>

<interfaces>
<!-- Contracts from Plans 03-01/03-02 this plan consumes, no redefinition. -->

From lib/scoring/completeness.ts (Plan 03-02):
`computeDataCompleteness(input: ScoringInput): "thin" | "complete"`.

From lib/scoring/stage.ts (Plan 03-02):
`getStage(totalScore: number): Stage` where `Stage = { level: number; label: string; whatThisMeans: string; whatsNext: string }`.

From lib/scoring/calculateScore.ts (Plan 03-01, behavior only — signature unchanged):
`calculateCategoryScores(input: ScoringInput): CategoryScores` now internally applies `getAdjustedWeights(input.companyProfile.industry)` — the existing call site in `route.ts` (`calculateCategoryScores(scoringInput)`) requires NO code change to benefit from this.

From app/api/assessments/route.ts (current, before this plan):
`const scoringInput: ScoringInput = { companyProfile, departments, techStack, dataReadiness, aiAdoption, automation, agentInterest, security, employeeReadiness };` already exists before the `prisma.assessment.create` call. The `create` call currently has the literal line `dataCompleteness: null,`.

From app/results/[id]/page.tsx (current, before this plan):
Server Component fetches `const assessment = await prisma.assessment.findUnique({ where: { id: params.id } });`. Currently renders `{assessment.stage}` (a plain persisted string) inside a `<span className="inline-block rounded-full border border-kx-navy px-4 py-1 kx-caption text-kx-navy">`, followed immediately by an unconditional `<p className="kx-caption text-kx-grey">Based on a fast-pass assessment; scores refine with more detail</p>`.

From app/api/assessments/route.test.ts (current, before this plan):
`function fullDepartments()` returns all 6 departments with `capabilities: dept.capabilities.slice(0, 2)`. `function fullBody(overrides)` builds a full valid 10-section POST body. `function post(body)` calls `POST` directly. `createdIds: string[]` collected for `afterAll` cleanup via `prisma.assessment.deleteMany`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist real dataCompleteness on submission</name>
  <files>app/api/assessments/route.ts, app/api/assessments/route.test.ts</files>
  <read_first>
    app/api/assessments/route.ts
    app/api/assessments/route.test.ts
    lib/scoring/completeness.ts
    prisma/schema.prisma
  </read_first>
  <behavior>
    - Test 1: POSTing `fullBody()` (the existing fixture — 2 capabilities × 6 departments = 12, plus 2 systems + 2 AI tools + 2 automation processes + 2 security items = 8; 20 of 68 total) then `GET`-ing the created id returns `dataCompleteness: "thin"` (20/68 ≈ 0.294 < 0.5).
    - Test 2: POSTing `fullBody({ departments: fullDepartments(5) })` (extend the existing `fullDepartments()` helper to accept an optional `capCount = 2` parameter, using `dept.capabilities.slice(0, capCount)` — this keeps every existing call site's behavior identical since the default stays 2) — 5 capabilities × 6 departments = 30, plus the same 8 from the other 4 sections = 38 of 68 — then `GET`-ing the created id returns `dataCompleteness: "complete"` (38/68 ≈ 0.559 ≥ 0.5).
  </behavior>
  <action>
    In `app/api/assessments/route.ts`, import `computeDataCompleteness` from `../../../lib/scoring/completeness`. In the `prisma.assessment.create` call, replace the literal `dataCompleteness: null,` line with `dataCompleteness: computeDataCompleteness(scoringInput),` (computed from the already-built `scoringInput` object, no new destructuring needed).

    In `app/api/assessments/route.test.ts`, change `function fullDepartments()` to `function fullDepartments(capCount = 2)` and change the body to use `dept.capabilities.slice(0, capCount)` instead of the hardcoded `2`. Add the 2 new test cases described in `<behavior>` inside the existing `describe("POST /api/assessments (full 10-section body)")` block, following the same `post(...)` / `GET(...)` / `createdIds.push(id)` pattern already used by the first test in that file.
  </action>
  <verify>
    <automated>npx vitest run app/api/assessments/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests pass
    - All 4 pre-existing tests in `route.test.ts` still pass unchanged
    - `grep -c "dataCompleteness: null" app/api/assessments/route.ts` returns 0
  </acceptance_criteria>
  <done>POST /api/assessments persists a real "thin"/"complete" dataCompleteness value computed from the submitted answers, replacing the previous hardcoded null.</done>
</task>

<task type="auto">
  <name>Task 2: Show stage guidance text + conditional completeness caption on results page</name>
  <files>app/results/[id]/page.tsx</files>
  <read_first>
    app/results/[id]/page.tsx
    lib/scoring/stage.ts
    lib/types/assessment.ts
  </read_first>
  <action>
    In `app/results/[id]/page.tsx`, import `{ getStage }` from `../../../lib/scoring/stage`. Immediately after the existing `if (!assessment) { notFound(); }` check, compute `const stage = getStage(assessment.totalScore);`.

    Replace the stage badge's `{assessment.stage}` text with `{stage.label}` (single source of truth computed fresh from `totalScore`, avoiding any drift from the persisted string — both are deterministically identical since `route.ts` computed `stage.label` the same way at submission time).

    Directly below the existing stage-label `<span>`, add a new element rendering both guidance sentences — e.g. a `<div className="flex flex-col gap-1">` containing two `<p className="kx-caption text-kx-grey">` elements, one rendering `{stage.whatThisMeans}` and one rendering `{stage.whatsNext}`.

    Change the existing unconditional fast-pass caption paragraph (`<p className="kx-caption text-kx-grey">Based on a fast-pass assessment; scores refine with more detail</p>`) so it renders only when the submission is thin: wrap it as `{assessment.dataCompleteness === "thin" && ( <p className="kx-caption text-kx-grey">Based on a fast-pass assessment; scores refine with more detail</p> )}` — the caption must be entirely absent from the rendered HTML (not just visually hidden) when `dataCompleteness` is `"complete"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` and `npm run build` both exit 0
    - `grep -c 'dataCompleteness === "thin"' app/results/[id]/page.tsx` returns 1
    - `grep -c "stage.whatThisMeans" app/results/[id]/page.tsx` and `grep -c "stage.whatsNext" app/results/[id]/page.tsx` each return 1
  </acceptance_criteria>
  <done>/results/[id] shows the computed stage's whatThisMeans/whatsNext text next to the stage label, and the fast-pass caption only appears in the rendered HTML when the submission's dataCompleteness is "thin".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: End-to-end industry/size/stage/completeness walkthrough</name>
  <files>None — gate only; Task 1, Task 2, and Plans 03-01/03-02 build everything this task verifies</files>
  <read_first>
    app/results/[id]/page.tsx
    app/api/assessments/route.ts
  </read_first>
  <action>
    Present the manual walkthrough steps below to the human and block on
    explicit approval (or an issue description) before this plan is
    considered done. No files are created or modified by this task itself.
  </action>
  <what-built>Industry- and size-adjusted category scoring (Plan 03-01), stage guidance text, and a conditional data-completeness caption (Plan 03-02 + this plan's wiring), all now visible on /results/[id].</what-built>
  <how-to-verify>
    1. Run `npm run dev` and submit two assessments via `/assessment` with identical answers except `companyProfile.industry` (e.g. "Manufacturing" vs "SaaS / Technology"). Confirm the two `/results/[id]` pages show visibly different total scores.
    2. Submit an assessment with `employeeBand` "1-10" selecting only 2-3 Technology systems, and a second with `employeeBand` "200+" selecting the same absolute number of systems. Confirm the "1-10" submission's score is not unfairly penalized relative to the "200+" one for having fewer absolute systems.
    3. On any `/results/[id]` page, confirm the stage badge is followed by one "what this means" sentence and one "what's next" sentence, both readable and specific to that stage.
    4. Submit a minimal-answers assessment (skip most optional checklist items — systems, AI tools, automated processes, security controls, department capabilities) and confirm the "Based on a fast-pass assessment; scores refine with more detail" caption appears. Submit a fully-detailed assessment (check most checklist items across all sections) and confirm the caption does NOT appear.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., a specific stage's guidance text renders oddly, or the caption doesn't toggle as expected).</resume-signal>
  <verify>
    <automated>MISSING — human gate, no automated check; approval is the gate itself</automated>
  </verify>
  <acceptance_criteria>
    - Human has explicitly approved all 4 verification steps above, or described issues that were resolved before this plan is marked done
  </acceptance_criteria>
  <done>Human has typed "approved" (or equivalent explicit confirmation) for the full industry/size/stage/completeness walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `POST /api/assessments` (existing, Plan 02-03) | Already-validated `ScoringInput` now also feeds `computeDataCompleteness` — no new external input surface, since this reads the same already-validated object used for `calculateCategoryScores`. |
| `/results/[id]` (existing, Plan 01-04) | Read-only server-rendered display of a persisted row — no new input surface; `getStage(assessment.totalScore)` operates on a number already computed and clamped 0-100 at submission time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-03-03 | Tampering | `app/api/assessments/route.ts` (`computeDataCompleteness` call) | accept | Reads only the already-validated `scoringInput` object built earlier in the same handler, after all 10 sections have passed their existing subset/range validation (Plan 02-03) — no new untrusted input reaches this function. |
| T-03-04 | Information Disclosure | `app/results/[id]/page.tsx` (`getStage` call) | accept | `getStage` operates on `assessment.totalScore`, a number already clamped to 0-100 and persisted at submission time; no new data is exposed beyond what the page already rendered (stage label was already shown). |
| T-03-SC | Tampering | npm package installs | n/a | No new npm packages are installed in this plan — Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `npx vitest run app/api/assessments/route.test.ts` — all tests (4 pre-existing + 2 new) pass
- `npx tsc --noEmit && npm run build` exit 0
- Human-verified walkthrough (Task 3) approved
</verification>

<success_criteria>
- ENG-04: `/results/[id]` shows one of the 6 named stages with its static "what this means"/"what's next" text, matching the computed total score.
- ENG-05: the fast-pass caption appears on `/results/[id]` exactly when fewer than half of the optional checklist items were touched, and is absent otherwise.
- Two submissions with identical raw ratings but different industries produce visibly different total scores (ENG-02, wired end-to-end).
- `npm run build`, `npm run lint`, and `npx vitest run` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/03-industry-size-adjusted-scoring-engine/03-03-SUMMARY.md` when done
</output>
